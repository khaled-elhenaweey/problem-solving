​
function SeriesSum(n) { 
    let zero = 0;
    let fraction = 0; 
    let denominator = 1;
​
    if (n === zero) {↔}  
​
    for (let i = 1; i < n; i++) {↔} 
​
    let number = 1 + fraction;
    return number.toFixed(2);
 }
​